/**
 * Row 02, "Observations made for experts" — an eye.
 *
 * The one row Figma leaves open, and the only mark in the set with no straight
 * edges, which is what keeps it apart from <EdgeWatch> in the banner: that one
 * is a camera housing, a box that happens to watch, while this is the act of
 * observing itself.
 */
export default function ObservationEye() {
  return (
    <svg viewBox="0 0 32 32" fill="none" aria-hidden="true" focusable="false">
      <path d="M4.2 16s4.6-7.4 11.8-7.4S27.8 16 27.8 16s-4.6 7.4-11.8 7.4S4.2 16 4.2 16Z" />
      <circle cx="16" cy="16" r="3.1" />
    </svg>
  );
}

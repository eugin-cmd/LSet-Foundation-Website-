/** Figma node 1993:3270 — the rail's own content. */

/** The "What's this about" options. Figma draws the select closed, showing
 *  "Select one", and names no options; these are the enquiry kinds the rail
 *  beside the form already sorts people into, so the two agree. */
export const ENQUIRY_KINDS = [
  "Partnership",
  "Press",
  "Education enquiry",
  "Support the work",
  "Something else",
];

/* The rail that stood beside the form is gone, by direction, and its content
   with it: a callout pointing species work at the challenge form, three desks
   to write to directly, and the campus address. They are in the history if any
   of them is wanted back. */
